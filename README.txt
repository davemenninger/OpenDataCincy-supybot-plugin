# OpenDataCincy supybot plugin

This plugin just fetches random items from the OpenDataCincy data set here: http://www.opendatacincy.org/api/

The command is `odc <type>` e.g. `@odc threeoneones` or `@odc arrests`.

The command `@odc types` will list the available types.
